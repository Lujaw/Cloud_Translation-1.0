<h1>Listing tasks</h1>

<table>
  <tr>
    <th>Content</th>
    <th>Reward</th>
  </tr>

<% @tasks.each do |task| %>
  <tr>
    <td><%= task.content %></td>
    <td><%= task.reward %></td>
    <td><%= link_to 'Translate', edit_task_path(task) %></td>
  </tr>
<% end %>
</table>

<br />

