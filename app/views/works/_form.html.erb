<%= form_for(@work) do |f| %>
  <% if @work.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work.errors.count, "error") %> prohibited this work from being saved:</h2>

      <ul>
      <% @work.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div id ="form">
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :from %><br />
    <%= select_tag "payment", options_for_select([ "English", "Nepali", "Hindi" ], "Nepali") %>
  </div>
  <div class="field">
    <%= f.label :to %><br />
    <%= select_tag "payment", options_for_select([ "English", "Nepali", "Hindi" ], "English") %>
  </div>
  <div class="field">
    <%= f.label :reward %><br />
    <%= f.text_field :reward %>
  </div>
  <div class="field">
    <%= f.label :additional_info %><br />
    <%= f.text_area :additional_info %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
</div>
<% end %>
